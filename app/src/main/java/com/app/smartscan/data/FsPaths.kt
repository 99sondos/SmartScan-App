package com.app.smartscan.data

/**
 * A helper object to store constant string values for Firestore collection paths.
 * This helps prevent typos and makes the code easier to maintain.
 */
object FsPaths {
    const val USERS = "users"
    const val PRODUCTS = "products"
    const val SCANS = "scans"
}
